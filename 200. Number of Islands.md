# 200. Number of Islands

## STEP1
```text
何もみずに5分以内に解いてみる
```
#### 考えたこと
```
すべての地点を確認
未訪問かつ島である場合island_cntを+1。
その地点からdfsを実施。未訪問の島のエリアをすべて探索する。
時間計算量　O(mn)
```
```cpp
class Solution {
 public:
  vector<vector<char>> grid;
  vector<vector<bool>> visited;
  int cnt = 0;
  int column;
  int row;
  vector<int> dx = {-1, 0, 1, 0};
  vector<int> dy = {0, -1, 0, 1};
  int numIslands(vector<vector<char>> &grid) {
    this->grid = grid;
    column = grid[0].size();
    row = grid.size();
    visited.resize(row, vector<bool>(column));
    for (int i = 0; i < row; i++) {
      for (int j = 0; j < column; j++) {
        if (!visited[i][j] && grid[i][j] == '1') {
          cnt++;
          numIslandsHelper(i, j);
        }
      }
    }
    return cnt;
  }

 private:
  void numIslandsHelper(const int &x, const int &y) {
    if (!(x < row && x >= 0 && y < column && y >= 0)) {
      return;
    }
    if (visited[x][y] || grid[x][y] != '1') {
      return;
    }
    visited[x][y] = true;
    if (grid[x][y] == '1') {
      for (int i = 0; i < 4; i++) {
        numIslandsHelper(x + dx[i], y + dy[i]);
      }
    }
  }
};

```
### 感想
```text
関数の引数とメンバ変数の名前がかぶってると、関数内では引数が優先される
ローカル変数のスコープの整理をしたい
dfs実行関数の命名
```
## STEP2
### プルリクやドキュメントを参照
```text
https://github.com/WNomunomu/leetcode/pull/1
https://github.com/quinn-sasha/leetcode/pull/18
https://github.com/colorbox/leetcode/pull/32
```
- bfsでも書いてみる
- std::vector<bool> の要素は1ビットを参照するプロキシオブジェクトを返すらしい
- https://github.com/Hurukawa2121/leetcode/pull/17#discussion_r1898916592
- https://cpprefjp.github.io/reference/vector/vector.html
- bool を 1 ビットで管理しているため、bool&（参照）は返せない
- そのため、動作がやや重い可能性がある
- std::vector<uint8_t> を使うことにする。
  https://cpprefjp.github.io/reference/cstdint/uint8_t.html
- 1や0を変数に置くのが可読性があがりそうでよさそう

#### bfs
```cpp
class Solution {
 public:
  struct RowToColumn {
    int row;
    int column;
  };
  int numIslands(vector<vector<char>>& grid) {
    const int width = grid[0].size();
    const int height = grid.size();
    char kSea = '0';
    char kLand = '1';
    int number_of_island = 0;
    vector<vector<uint8_t>> visited(height, vector<uint8_t>(width));
    queue<RowToColumn> next_coordinates;
    vector<int> next_row = {-1, 0, 1, 0};
    vector<int> next_column = {0, -1, 0, 1};
    auto IsIsland = [&]() {
      while(!next_coordinates.empty()) {
        auto [row, column] = next_coordinates.front();
        next_coordinates.pop();
        if (grid[row][column] == kSea) {
            continue;
        }
        if (visited[row][column]) {
            continue;
        }
        visited[row][column] = 1;
        for (int i = 0; i < 4; i++) {
          if (!(row + next_row[i] < height && row + next_row[i] >=0)) {
            continue;
          }
          if (!(column + next_column[i] < width && column + next_column[i] >= 0)) {
            continue;
          }
          next_coordinates.push({row + next_row[i], column + next_column[i]});
        }
      }
    };
    for(int i = 0; i < height; i++) {
      for (int j = 0; j < width; j++) {
        if (grid[i][j] == kLand && !visited[i][j]) {
            next_coordinates.push({i, j});
            number_of_island++;
            IsIsland();
        } 
      }
    }
    return number_of_island;
  }
};
```
#### STEP1やりなおし
```cpp
class Solution {
 public:
  const char is_land = '1';
  const char is_sea = '0';
  int height, width;
  vector<vector<uint8_t>> visited;
  int numIslands(vector<vector<char>>& grid) {
    height = grid.size();
    width = grid[0].size();
    visited.resize(height, vector<uint8_t> (width));
    int lands = 0;
    for (int row = 0; row < height; row++) {
      for (int column = 0; column < width; column++) {
        if (grid[row][column] == is_land && !visited[row][column]) {
            lands++;
            IsIsland(row, column, grid);
        }
      }  
    }
    return lands;
  }

 private:
  const vector<int> next_row = {-1, 0, 1, 0};
  const vector<int> next_column = {0, -1, 0, 1};
  void IsIsland(int row, int column, const vector<vector<char>>& grid) {
    if (!(row >= 0 && row < height && column >= 0 && column < width)) {
      return;
    }
    if (visited[row][column]) {
        return;
    }
    if (grid[row][column] == is_sea) {
        return;
    }
    visited[row][column] = 1;
    for (int i = 0; i < 4; i++) {
        IsIsland(row + next_row[i], column + next_column[i], grid);
    }
  }
};
```
### 感想
```text
lambdaで再帰を書いてみたかったがうまくかけず。
時間があるときに調査したい
```
## STEP3
### ミスなく三回書く
```cpp
class Solution {
 public:
  const char is_land = '1';
  const char is_sea = '0';
  int height, width;
  vector<vector<uint8_t>> visited;
  int numIslands(vector<vector<char>>& grid) {
    height = grid.size();
    width = grid[0].size();
    visited.resize(height, vector<uint8_t> (width));
    int lands = 0;
    for (int row = 0; row < height; row++) {
      for (int column = 0; column < width; column++) {
        if (grid[row][column] == is_land && !visited[row][column]) {
            lands++;
            IsIsland(row, column, grid);
        }
      }  
    }
    return lands;
  }

 private:
  const vector<int> next_row = {-1, 0, 1, 0};
  const vector<int> next_column = {0, -1, 0, 1};
  void IsIsland(int row, int column, const vector<vector<char>>& grid) {
    if (!(row >= 0 && row < height && column >= 0 && column < width)) {
      return;
    }
    if (visited[row][column]) {
        return;
    }
    if (grid[row][column] == is_sea) {
        return;
    }
    visited[row][column] = 1;
    for (int i = 0; i < 4; i++) {
        IsIsland(row + next_row[i], column + next_column[i], grid);
    }
  }
};
```
### 感想
```text
lamda, scope, 定数等について復習する
