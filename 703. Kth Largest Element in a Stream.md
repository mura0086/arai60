# 703. Kth Largest Element in a Stream

## STEP1
- 何もみずに解いてみる
- ぱっと入力と期待される出力がわからなかったので答えをみた。

```cpp
class KthLargest {
    priority_queue<int, vector<int>, greater<int>> scores;
    int k;
public:
    KthLargest(int k, vector<int>& nums) {
        this->k = k;
        for (int num : nums) {
            add(num);
        }
    }
    
    int add(int val) {
        if (scores.size() < k || scores.top() < val) {
            scores.push(val);
            if (scores.size() > k) {
                scores.pop();
            }
        }
        return scores.top();
    }
};
```
#### 考えたこと
- ヒープを利用して解くようだった
- priority_queのサイズをk以下に絞ることで一番上の要素がk番目の値となる

#### 感想
- 回答をみたら理解できたがこの発想にいたらなかった。
- キューに追加する際の条件文は必要か


## STEP2
### プルリクやドキュメントを参照
- キューに追加する際の条件文は不要だった
- addのなかでキューのサイズがkより大きい場合はキューからポップしている
- 入れてから出すか、そもそもはじくか
- 条件を理解する労力が無駄に感じた
- 変数名をheapやqueueとしている人が多かった印象

```cpp
class KthLargest {
    priority_queue<int, vector<int>, greater<int>> scores;
    int k;
public:
    KthLargest(int k, vector<int>& nums) {
        this->k = k;
        if (k > 0) {
            for (int num : nums) {
                add(num);
            }
        }
    }
    
    int add(int val) {
        scores.push(val);
        if (scores.size() > k) {
            scores.pop();
        }
        return scores.top();
    }
};
```
#### 感想
- すっきりして読みやすくなった、動きもイメージしやすい
- kが0以下の条件分岐をつけたがこの問題に限ればなくても問題ない、状況により選択したい

## STEP2
### ミスなく三回書く

#### 感想
ヒープの中身を確認する必要がある
